<!DOCTYPE html>
<html>
<head>
    <title>Danh sách tất cả các tuyến đường và tổng lượt đi</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h2>Danh sách tất cả các tuyến đường và tổng lượt đi</h2>

        <div style="margin-top: 20px;">
            <ul class="list-group">
                @foreach (var route in ViewData["AllRoutes"] as IEnumerable<dynamic>)
                {
                    <li class="list-group-item">
                        <strong>@route.Route</strong>: @route.TicketsCount lượt
                    </li>
                }
            </ul>
        </div>
    </div>
</body>
</html>
